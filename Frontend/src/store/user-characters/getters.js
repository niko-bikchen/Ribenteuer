export function getUserCharactersDescriptions(state) {
  return [...state.charactersDescriptions];
}

export function getClassesDescriptions(state) {
  return [...state.classesDescriptions];
}

export function getClasses(state) {
  return [...state.classes];
}
